# No ignores: repository is configured to include all files per user request.
# NOTE: This intentionally leaves the repo without ignores. Be aware this will
# include node_modules, vendor, .env and other local files which can be large
# and may contain sensitive data. If you want to ignore specific files later,
# edit this file accordingly.
